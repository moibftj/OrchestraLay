export function DiffReview() {
  return (
    <section className="panel">
      <h2>Diff Review</h2>
      <p>Pending diffs, blocked warnings, and approval actions will render here once the diff router is wired.</p>
    </section>
  )
}